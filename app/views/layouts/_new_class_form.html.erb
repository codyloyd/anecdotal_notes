<%= form_for @new_student_group, url: student_group_index_path(@new_student_group), method: "post" do |f| %>
  <%= f.text_field :name, placeholder: "Class Name" %>
  <%= f.submit "Create New Class" %>
<% end %>
