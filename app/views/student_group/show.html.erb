<h2>students:</h2>
<ul>
  <% @students.each do |student| %>
    <li><%= link_to student.name, student %> | <%= student.notes.count %> notes</li>
  <% end %>
</ul>
<%= render "/layouts/new_student_form" %>
<%= link_to "Edit Class", edit_student_group_path %>,
<%= link_to "Delete Class", student_group_path, method: "delete" %>
